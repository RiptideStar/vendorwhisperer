
import { useState } from "react";
import { Card } from "@/components/ui/card";

const ShipmentMap = () => {
  return (
    <div className="p-6">
      <h2 className="text-lg font-semibold mb-4">Shipment Tracking</h2>
      <div className="h-[300px] w-full rounded-lg bg-accent flex items-center justify-center">
        <p className="text-muted-foreground">Interactive shipment map coming soon</p>
      </div>
    </div>
  );
};

export default ShipmentMap;
